{{- $length := len .Values.services }}
{{- range $key, $value := .Values.services }}
{{- if $value.phpFPMMetricsExporter.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  {{- if eq $length 1 }}
  name: {{ template "rds_exporter_hf.fullname" $ }}-fpm-metrics
  {{- else }}
  name: {{ template "rds_exporter_hf.fullname" $ }}-{{ $key }}-fpm-metrics
  {{- end }}
  labels:
    app: {{ template "rds_exporter_hf.name" $ }}-{{ $key }}
    chart: {{ template "rds_exporter_hf.chart" $ }}
    release: {{ $.Release.Name }}
    heritage: {{$.Release.Service }}
    tribe: {{ $.Values.tribe }}
    squad: {{ $.Values.squad }}
{{- with $value.annotations }}
  annotations:
{{ toYaml . | indent 4}}
{{- end }}
spec:
  type: {{ $value.type }}
  ports:
    - port: 80
      targetPort: {{ $value.phpFPMMetricsExporter.tcpPort }}
      protocol: TCP
      name: {{ $value.phpFPMMetricsExporter.portName }}
  selector:
    app: {{ template "rds_exporter_hf.name" $ }}-{{ $key }}
    release: {{ $.Release.Name }}
{{- end }}
{{- end }}

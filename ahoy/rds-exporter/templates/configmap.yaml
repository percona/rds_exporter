{{- if .Values.configMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "rds-exporter.fullname" . }}
  labels:
    app: {{ template "rds-exporter.name" . }}
    chart: {{ template "rds-exporter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    tribe: {{ .Values.tribe }}
    squad: {{ .Values.squad }}

data:
{{ toYaml .Values.configMap | indent 2 }}
{{- end }}

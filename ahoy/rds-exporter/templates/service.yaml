{{- $length := len .Values.services }}
{{- range $key, $value := .Values.services }}
{{- if $value.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  {{- if eq $length 1 }}
  name: {{ template "rds-exporter.fullname" $ }}-k8s
  {{- else }}
  name: {{ template "rds-exporter.fullname" $ }}-{{ $key }}-k8s
  {{- end }}
  labels:
    app: {{ template "rds-exporter.name" $ }}-{{ $key }}
    chart: {{ template "rds-exporter.chart" $ }}
    release: {{ $.Release.Name }}
    heritage: {{$.Release.Service }}
    tribe: {{ $.Values.tribe }}
    squad: {{ $.Values.squad }}
{{- with $value.annotations }}
  annotations:
{{ toYaml . | indent 4}}
{{- end }}
spec:
  type: {{ $value.type }}
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rds-exporter.name" $ }}-{{ $key }}
    release: {{ $.Release.Name }}
{{- end }}
{{- end }}
